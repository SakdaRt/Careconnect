---
description: Commit และ push งานขึ้น main
---

## ขั้นตอน

1. ตรวจสอบไฟล์ที่เปลี่ยนแปลง
// turbo
2. `git add -A`
// turbo
3. `git commit -m "type(scope): description"` — ใช้ format ตาม .windsurfrules
4. อัพเดท PROGRESS.md ตาม workflow `update-progress`
