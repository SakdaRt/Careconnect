# CareConnect — UX Flow Analysis & Improvement Recommendations

> วิเคราะห์ ณ วันที่ 21 กุมภาพันธ์ 2569  
> เป้าหมาย: ทำความเข้าใจ flow การใช้งานจริงของ user แต่ละกลุ่ม และระบุจุดที่ควรปรับปรุง

---

## 1. ภาพรวมระบบ

CareConnect เป็นแพลตฟอร์มจับคู่ **ผู้ว่าจ้าง (Hirer)** ที่ต้องการหาคนดูแลผู้สูงอายุ/ผู้ป่วย กับ **ผู้ดูแล (Caregiver)** ที่ต้องการรับงาน โดยมีระบบกระเป๋าเงิน, KYC, แชท, และข้อพิพาทในตัว

### กลุ่มผู้ใช้
| กลุ่ม | คำอธิบาย | เส้นทางหลัก |
|---|---|---|
| **Hirer (Guest)** | สมัครด้วยอีเมล ว่าจ้างได้อย่างเดียว | `/hirer/home` |
| **Member (Hirer + Caregiver)** | สมัครด้วยเบอร์โทร เปลี่ยนบทบาทได้ | `/hirer/home` หรือ `/caregiver/jobs/feed` |
| **Admin** | จัดการระบบ | `/admin/dashboard` |

---

## 2. Flow การสมัครและเข้าสู่ระบบ

### 2.1 การสมัครสมาชิก

```
Landing Page (/)
    │
    ├─► [สมัครสมาชิก] → /register
    │       │
    │       ├─► Google OAuth  → /auth/callback → เลือก Role → Consent → Home
    │       │
    │       ├─► บัญชีแขก (Email)  → /register/guest
    │       │       └─► กรอก email + password → สมัครสำเร็จ
    │       │               └─► /select-role (เลือก hirer เท่านั้น)
    │       │                       └─► /register/consent → /hirer/home
    │       │
    │       └─► บัญชีสมาชิก (Phone)  → /register/member
    │               └─► กรอกเบอร์ → OTP SMS → สมัครสำเร็จ
    │                       └─► /select-role (เลือก hirer หรือ caregiver)
    │                               └─► /register/consent → Home
    │
    └─► [เข้าสู่ระบบ] → /login
            │
            ├─► Google OAuth → /auth/callback
            ├─► Email + Password → /select-role (ถ้ายังไม่มี role active)
            └─► Phone + OTP → /select-role
```

### 2.2 Trust Level System

| Level | เงื่อนไข | สิทธิ์ |
|---|---|---|
| **L0** | แค่สมัคร | สร้าง draft งาน, เติมเงิน |
| **L1** | ยืนยันเบอร์โทร | สร้างงาน low_risk, ดูบัญชีธนาคาร |
| **L2** | L1 + KYC อนุมัติ | สร้างงาน high_risk, ถอนเงิน |
| **L3** | L2 + บัญชีธนาคารยืนยัน + Trust Score ≥ 80 | สิทธิ์สูงสุด |

---

## 3. Flow ผู้ว่าจ้าง (Hirer)

### 3.1 การสร้างงาน (4 ขั้นตอน)

```
/hirer/home
    │
    [+ สร้างงาน] → /hirer/create-job
    │
    ├─► Step 1: ข้อมูลหลัก
    │       - เลือกผู้รับการดูแล (Care Recipient) หรือสร้างใหม่
    │       - เลือกประเภทงาน (6 template: ดูแลทั่วไป, พาไปโรงพยาบาล,
    │         หลังผ่าตัด, สมองเสื่อม, ติดเตียง, อุปกรณ์การแพทย์)
    │       - ตอบ Dynamic Questions ตาม template ที่เลือก
    │
    ├─► Step 2: งานและคุณสมบัติ
    │       - ปรับ Tasks ที่ต้องทำ (auto-suggest จาก template + คำตอบ)
    │       - ทักษะที่ต้องการ, อุปกรณ์ที่มี, ข้อควรระวัง
    │
    ├─► Step 3: เวลาและสถานที่
    │       - วันเวลาเริ่ม-สิ้นสุด
    │       - สถานที่ (Google Places Autocomplete)
    │       - ราคาต่อชั่วโมง + จำนวนชั่วโมง
    │
    └─► Step 4: ตรวจทาน → บันทึก (draft) หรือ เผยแพร่ทันที
```

### 3.2 การจัดการงาน

```
/hirer/home (รายการงานทั้งหมด)
    │
    ├─► กรองตาม: ทั้งหมด / รอตอบรับ / กำลังดำเนินการ / เสร็จแล้ว / ยกเลิก
    │
    ├─► คลิกงาน → /jobs/:id (Job Detail)
    │       │
    │       ├─► [เผยแพร่] (draft → published, ต้องการ L1+)
    │       ├─► [ยกเลิก] (พร้อมระบุเหตุผล)
    │       ├─► [แชท] → /chat/:jobId (เมื่อมี caregiver assigned)
    │       ├─► [เปิดข้อพิพาท] → /dispute/:disputeId
    │       └─► [ดูใบเสร็จ] → /hirer/wallet/receipt/:jobId (เมื่องานเสร็จ)
    │
    └─► [ค้นหาผู้ดูแล] → /hirer/search-caregivers
            └─► ดูโปรไฟล์ → [สร้างงานให้ผู้ดูแลนี้] → /hirer/create-job?preferred_caregiver_id=...
```

### 3.3 กระเป๋าเงิน Hirer

```
/hirer/wallet
    │
    ├─► ยอดคงเหลือ (available + held)
    ├─► [เติมเงิน] → Modal → QR Code PromptPay (mock)
    ├─► [ประวัติ] → /hirer/wallet/history
    └─► [บัญชีธนาคาร] → /wallet/bank-accounts (ต้องการ L1+)
```

---

## 4. Flow ผู้ดูแล (Caregiver)

### 4.1 การหางาน

```
/caregiver/jobs/feed (Job Feed)
    │
    ├─► กรองตาม: ประเภทงาน, เรียงตาม (ใหม่สุด / ค่าจ้างสูง-ต่ำ)
    ├─► Toggle: ซ่อน/แสดงโปรไฟล์ต่อสาธารณะ
    │
    ├─► คลิกงาน → /caregiver/jobs/:id/preview (Job Preview)
    │       │
    │       ├─► ดูรายละเอียดงาน, tasks, ทักษะที่ต้องการ
    │       ├─► [รับงาน] (ต้องการ L1+, trust_level ≥ min_trust_level ของงาน)
    │       └─► [ดูงานเต็ม] → /jobs/:id
    │
    └─► [งานของฉัน] → /caregiver/jobs/my-jobs
            │
            ├─► รายการงานที่รับแล้ว
            ├─► [Check-in] (เมื่อถึงเวลา)
            ├─► [Check-out] (เมื่อทำงานเสร็จ)
            ├─► [แชท] → /chat/:jobId
            └─► [เปิดข้อพิพาท] → /dispute/:disputeId
```

### 4.2 กระเป๋าเงิน Caregiver

```
/caregiver/wallet
    │
    ├─► ยอดคงเหลือ (available + held)
    ├─► [ถอนเงิน] (ต้องการ L2+, มีบัญชีธนาคารที่ยืนยันแล้ว)
    ├─► [ประวัติรายได้] → /caregiver/wallet/history
    │       └─► คลิกรายการ → /caregiver/wallet/earning/:jobId
    └─► [บัญชีธนาคาร] → /wallet/bank-accounts (ต้องการ L1+)
```

---

## 5. Flow ยืนยันตัวตน (KYC)

```
/settings → [ยืนยันตัวตน] → /kyc
    │
    ├─► Step 1: อัปโหลดเอกสาร (บัตรประชาชน ด้านหน้า + ด้านหลัง)
    ├─► Step 2: สแกนใบหน้า (Selfie ผ่านกล้อง หรืออัปโหลด)
    ├─► Step 3: ข้อมูลส่วนตัว (ชื่อ-นามสกุล, เลขบัตร)
    └─► Step 4: ตรวจสอบ & ส่ง → รอ Admin อนุมัติ
            └─► เมื่ออนุมัติ: trust_level → L2
```

---

## 6. Flow แชทและข้อพิพาท

```
/chat/:jobId (Chat Room)
    │
    ├─► แชทระหว่าง Hirer ↔ Caregiver
    ├─► แสดงข้อมูลงาน (สถานะ, เวลา, สถานที่)
    └─► ปุ่ม action ตามสถานะงาน

/dispute/:disputeId (Dispute Chat)
    │
    ├─► แชทระหว่าง Hirer / Caregiver / Admin
    └─► Admin เป็นผู้ตัดสิน
```

---

## 7. Flow Admin

```
/admin/dashboard   → สถิติภาพรวม (users, jobs, revenue)
/admin/users       → จัดการผู้ใช้ (inline edit, ban, wallet view)
/admin/jobs        → จัดการงาน
/admin/disputes    → จัดการข้อพิพาท
/admin/financial   → รายการการเงิน
/admin/reports     → รายงานสรุป + ledger
/admin/settings    → ตั้งค่าระบบ
```

---

## 8. การวิเคราะห์ UX — จุดที่ User พบปัญหา

### 🔴 ปัญหาระดับสูง (กระทบ conversion โดยตรง)

#### 8.1 Onboarding ซับซ้อนเกินไปสำหรับ Hirer ใหม่
- **ปัญหา:** User ต้องผ่าน 5 หน้าก่อนถึงหน้าหลัก: Register → Guest/Member → กรอกข้อมูล → เลือก Role → Consent → ตั้งชื่อโปรไฟล์ → Home
- **ผลกระทบ:** Drop-off สูงในขั้นตอนแรก โดยเฉพาะ Hirer ที่แค่อยากลองใช้
- **แนวทางแก้:** Progressive onboarding — ให้เข้าถึง home ได้ก่อน แล้วค่อย prompt ให้กรอกข้อมูลเพิ่มเมื่อจำเป็น

#### 8.2 ไม่มีหน้า "ค้นหาผู้ดูแล" ที่ใช้งานได้จริงก่อนสมัคร
- **ปัญหา:** `/hirer/search-caregivers` ต้อง login ก่อน แต่ user ยังไม่รู้ว่ามีผู้ดูแลที่ตรงกับความต้องการหรือเปล่า
- **ผลกระทบ:** ไม่มีแรงจูงใจให้สมัคร
- **แนวทางแก้:** ทำ public preview ของ caregiver profiles บน Landing Page

#### 8.3 ฟอร์มสร้างงาน 4 ขั้นตอนยาวเกินไป
- **ปัญหา:** CreateJobPage มี 4 steps + dynamic questions จำนวนมาก user ที่ไม่คุ้นเคยกับศัพท์การแพทย์อาจสับสน (เช่น "NG tube", "catheter care")
- **ผลกระทบ:** Abandon rate สูงในหน้าสร้างงาน
- **แนวทางแก้:** 
  - เพิ่ม tooltip/คำอธิบายภาษาชาวบ้านสำหรับ task ที่ซับซ้อน
  - บันทึก draft อัตโนมัติทุก step
  - ให้ข้ามขั้นตอนที่ไม่จำเป็นได้ (optional sections)

#### 8.4 Caregiver ไม่รู้ว่าตัวเองมีสิทธิ์รับงานไหนบ้าง
- **ปัญหา:** Job Feed แสดงงานทั้งหมด แต่งานที่ trust_level ไม่ผ่านจะแสดง `eligible: false` โดยไม่มีคำอธิบายชัดเจนว่าต้องทำอะไรเพิ่ม
- **ผลกระทบ:** Caregiver ใหม่ (L0/L1) ไม่รู้ว่าต้องยืนยัน KYC ก่อน
- **แนวทางแก้:** Banner/CTA ชัดเจนบน Job Feed "ยืนยัน KYC เพื่อรับงานประเภทนี้"

---

### 🟡 ปัญหาระดับกลาง (กระทบ retention และ trust)

#### 8.5 ไม่มีระบบ Rating/Review
- **ปัญหา:** ไม่มีหน้าหรือ flow สำหรับ Hirer ให้คะแนน Caregiver หลังงานเสร็จ (มีแค่ `trust_score` ใน backend แต่ไม่มี UI)
- **ผลกระทบ:** Caregiver ที่ดีไม่ได้รับการยืนยันคุณภาพ, Hirer ไม่มีข้อมูลช่วยตัดสินใจ
- **แนวทางแก้:** เพิ่มหน้า Review หลัง job completed, แสดง rating บน caregiver profile

#### 8.6 การแจ้งเตือนไม่ครอบคลุม
- **ปัญหา:** มีระบบ notification แต่ไม่มี push notification จริง (ต้องเปิดแอปเอง) และ TopBar polling ทุก 30 วินาทีเท่านั้น
- **ผลกระทบ:** Caregiver อาจพลาดงานใหม่, Hirer ไม่รู้ว่ามีคนรับงาน
- **แนวทางแก้:** เพิ่ม Web Push Notification หรืออย่างน้อย real-time via WebSocket (Socket.IO มีอยู่แล้ว)

#### 8.7 ไม่มีหน้าโปรไฟล์ Caregiver แบบ Public
- **ปัญหา:** Hirer ค้นหา caregiver ได้ แต่ไม่มีหน้า `/caregiver/:id` ที่แสดงโปรไฟล์แบบสาธารณะ
- **ผลกระทบ:** Hirer ไม่สามารถดูรายละเอียด caregiver ก่อนตัดสินใจ
- **แนวทางแก้:** สร้างหน้า Public Caregiver Profile

#### 8.8 SettingsPage แทบว่างเปล่า
- **ปัญหา:** `/settings` มีแค่ลิงก์ไป FAQ, Contact, About, Notifications, KYC — ไม่มีการตั้งค่าจริง
- **ผลกระทบ:** User ที่ต้องการเปลี่ยน password, ลบบัญชี, หรือตั้งค่า notification ไม่พบ
- **แนวทางแก้:** เพิ่ม: เปลี่ยนรหัสผ่าน, ตั้งค่าการแจ้งเตือน, ลบ/ปิดบัญชี

#### 8.9 ไม่มีปุ่มกลับ/Home บนหน้าที่ซ่อน BottomBar
- **ปัญหา:** หน้าอย่าง `/kyc`, `/chat/:jobId`, `/dispute/:disputeId` ซ่อน BottomBar แต่ไม่มีปุ่มกลับที่ชัดเจน
- **ผลกระทบ:** User ติดอยู่ในหน้านั้น ต้องใช้ปุ่ม Back ของ browser
- **แนวทางแก้:** เพิ่ม Back button ใน MainLayout เมื่อ `showBottomBar=false`

#### 8.10 QR Code เติมเงินเป็น Mock
- **ปัญหา:** ระบบเติมเงินใช้ QR Code ที่ generate จาก algorithm สุ่ม ไม่ใช่ PromptPay จริง
- **ผลกระทบ:** User ที่ไม่รู้อาจพยายาม scan และสับสน
- **แนวทางแก้:** ระบุชัดเจนว่าเป็น "ระบบทดสอบ" หรือเชื่อมต่อ payment gateway จริง

---

### 🟢 ปัญหาระดับต่ำ (UX polish)

#### 8.11 ไม่มี Empty State ที่ดีพอ
- **ปัญหา:** หน้า Job Feed, My Jobs, Wallet History เมื่อไม่มีข้อมูลแสดงแค่ข้อความ "ไม่พบ..." ไม่มี CTA
- **แนวทางแก้:** Empty state พร้อมปุ่ม action เช่น "สร้างงานแรกของคุณ" หรือ "ยืนยันตัวตนเพื่อรับงาน"

#### 8.12 ไม่มี Onboarding Checklist
- **ปัญหา:** User ใหม่ไม่รู้ว่าต้องทำอะไรบ้างเพื่อใช้งานได้เต็มที่
- **แนวทางแก้:** Progress checklist บน Home: ✓ สมัครแล้ว → ✓ ตั้งชื่อโปรไฟล์ → ✓ ยืนยันเบอร์ → ✓ KYC → ✓ เพิ่มบัญชีธนาคาร

#### 8.13 Care Recipients ไม่ถูก highlight ในการสร้างงาน
- **ปัญหา:** Hirer ต้องสร้าง Care Recipient แยกก่อน แต่ถ้ายังไม่มีจะเห็นแค่ dropdown ว่าง ไม่มี prompt ให้สร้างใหม่ทันที
- **แนวทางแก้:** ถ้า dropdown ว่าง ให้แสดง inline form สร้าง Care Recipient ได้เลย

#### 8.14 ไม่มีการยืนยันก่อนออกจากฟอร์มสร้างงาน
- **ปัญหา:** กด Back ระหว่างกรอก CreateJobPage ข้อมูลหายหมด (ไม่มี unsaved changes warning)
- **แนวทางแก้:** `beforeunload` event หรือ React Router blocker

#### 8.15 ไม่มีหน้า Caregiver Profile ใน Bottom Navigation
- **ปัญหา:** Caregiver ต้องไปที่ `/caregiver/profile` ผ่าน URL โดยตรง ไม่มีลิงก์ใน Bottom Bar ที่ชัดเจน
- **แนวทางแก้:** เพิ่ม tab "โปรไฟล์" ใน Bottom Bar ของ caregiver

---

## 9. สรุปลำดับความสำคัญในการแก้ไข

| ลำดับ | รายการ | ผลกระทบ | ความยาก |
|---|---|---|---|
| 1 | Rating/Review system | สูงมาก | กลาง |
| 2 | Public Caregiver Profile page | สูง | ต่ำ |
| 3 | KYC CTA บน Job Feed สำหรับ L0 | สูง | ต่ำ |
| 4 | Back/Home button บนหน้าที่ซ่อน BottomBar | กลาง | ต่ำมาก |
| 5 | Empty state + CTA บนทุกหน้า | กลาง | ต่ำ |
| 6 | Onboarding checklist บน Home | กลาง | ต่ำ |
| 7 | Real-time notification (WebSocket) | กลาง | กลาง |
| 8 | SettingsPage เพิ่มฟีเจอร์จริง | กลาง | กลาง |
| 9 | Unsaved changes warning บน CreateJobPage | ต่ำ | ต่ำ |
| 10 | Payment gateway จริง (แทน mock QR) | สูง | สูง |
| 11 | Simplify onboarding flow | สูง | สูง |
| 12 | Public caregiver search (ก่อน login) | สูง | กลาง |

---

## 10. Feature ที่มีใน Backend แต่ยังไม่มี Frontend

| Feature | Backend | Frontend | หมายเหตุ |
|---|---|---|---|
| Rating/Review | ❓ ยังไม่ชัดเจน | ❌ | ต้องสร้างทั้งคู่ |
| Caregiver Public Profile | ✅ มี API | ❌ | ต้องสร้างหน้า |
| Dispute resolution (admin ตัดสิน) | ✅ | ✅ บางส่วน | Admin UI ยังไม่ครบ |
| Trust Score update | ✅ | ❌ | ไม่มี UI ให้ user เห็น score ของตัวเอง |
| Withdrawal approval flow | ✅ | ✅ บางส่วน | Admin ต้อง approve แต่ UI ยังไม่สมบูรณ์ |
| First job waiver | ✅ | ❌ | ไม่มีการแสดงให้ user รู้ว่ามีสิทธิ์นี้ |
| Caregiver availability schedule | ✅ (ใน profile) | ✅ | แต่ไม่ถูก match กับ job schedule |

---

## 11. ข้อสังเกตเพิ่มเติม

### ความไม่สอดคล้องของ UX
- **Role switching:** User เปลี่ยน role ได้ผ่าน `/select-role` แต่ไม่มีปุ่มเปลี่ยน role ที่ชัดเจนใน UI หลัก (ต้องรู้ URL)
- **Account type vs Role:** ระบบแยก `account_type` (guest/member) กับ `role` (hirer/caregiver) แต่ UI อธิบายไม่ชัด ทำให้ user สับสน
- **Favorites page:** มีหน้า `/hirer/favorites` แต่ไม่มีปุ่ม "บันทึก" บน caregiver card ใน search results

### ความปลอดภัยและ Privacy
- ข้อมูลส่วนตัว (email, phone) ถูก mask ใน TopBar แล้ว ✅
- Chat ใช้ display_name แทน email ✅
- แต่ยังไม่มีการแจ้ง user ว่าข้อมูลใดบ้างที่ caregiver/hirer อีกฝ่ายมองเห็น

---

*เอกสารนี้เป็นการวิเคราะห์จากโค้ดและ flow ปัจจุบัน ไม่ได้แก้ไขโค้ดใดๆ งานในลำดับถัดไปควรเริ่มจากรายการที่ 1-4 ในตารางลำดับความสำคัญ*
